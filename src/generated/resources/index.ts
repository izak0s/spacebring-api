// AUTO-GENERATED by codegen/generate-facade.ts — DO NOT EDIT.
// Regenerate with `npm run generate:facade`.
import type { Client } from "openapi-fetch";
import type { SpacebringDefaults } from "../../core.js";
import type { paths } from "../schema.js";
import { createAltcurrencies as altCurrenciesGroup } from "./altCurrencies.js";
import { createBenefits as benefitsGroup } from "./benefits.js";
import { createBilling as billingGroup } from "./billing.js";
import { createCommunity as communityGroup } from "./community.js";
import { createContracts as contractsGroup } from "./contracts.js";
import { createDiscounts as discountsGroup } from "./discounts.js";
import { createEvents as eventsGroup } from "./events.js";
import { createFeed as feedGroup } from "./feed.js";
import { createFloors as floorsGroup } from "./floors.js";
import { createGuides as guidesGroup } from "./guides.js";
import { createLocations as locationsGroup } from "./locations.js";
import { createNetworks as networksGroup } from "./networks.js";
import { createPlans as plansGroup } from "./plans.js";
import { createRegistrations as registrationsGroup } from "./registrations.js";
import { createResources as resourcesGroup } from "./resources.js";
import { createShop as shopGroup } from "./shop.js";
import { createSubscriptions as subscriptionsGroup } from "./subscriptions.js";
import { createSupport as supportGroup } from "./support.js";
import { createTransactions as transactionsGroup } from "./transactions.js";
import { createVisitors as visitorsGroup } from "./visitors.js";

export function createResources(client: Client<paths>, defaults: SpacebringDefaults) {
  return {
    altCurrencies: altCurrenciesGroup(client, defaults),
    benefits: benefitsGroup(client, defaults),
    billing: billingGroup(client, defaults),
    community: communityGroup(client, defaults),
    contracts: contractsGroup(client, defaults),
    discounts: discountsGroup(client, defaults),
    events: eventsGroup(client, defaults),
    feed: feedGroup(client, defaults),
    floors: floorsGroup(client, defaults),
    guides: guidesGroup(client, defaults),
    locations: locationsGroup(client, defaults),
    networks: networksGroup(client, defaults),
    plans: plansGroup(client, defaults),
    registrations: registrationsGroup(client, defaults),
    resources: resourcesGroup(client, defaults),
    shop: shopGroup(client, defaults),
    subscriptions: subscriptionsGroup(client, defaults),
    support: supportGroup(client, defaults),
    transactions: transactionsGroup(client, defaults),
    visitors: visitorsGroup(client, defaults),
  };
}

export type SpacebringResources = ReturnType<typeof createResources>;
